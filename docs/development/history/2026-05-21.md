# 2026-05-21

## Summary

- Aligned the continuation docs around the actual active branch state.
- Confirmed that `v2/core-refactor` is the current gameplay branch.
- Clarified that older v1 gameplay is archived reference content, not the active game.
- Implemented the roadmap execution pass across the active branch runtime.

## What Changed

- Rewrote `docs/development/start-of-day.md` so it now reflects the live branch/runtime instead of the older expanded runtime summary.
- Updated `docs/development/current-state.md` wording so branch ownership is explicit:
  - active work happens on `v2/core-refactor`
  - archived v1 content lives on `main` and under `archive/v1/`
  - the live runtime wording no longer implies that v1 or other archived systems are still active
- Rewrote the active process docs so scope, architecture, and roadmap guidance now match the live branch runtime.
- Renamed `docs/design/game-direction-v3.md` to `docs/design/game-direction-current.md` so the active direction doc no longer reads like an old phase label.
- Marked older implementation-plan docs as historical records instead of live instructions.
- Added visible `OUTDATED` notes to:
  - `docs/design/weapons-passives-balance.xlsx`
  - `docs/design/enemies-arenas-modifiers-balance.xlsx`
- Implemented the active roadmap slices in runtime code:
  - flattened economy to `1g` per enemy and removed the survival bonus
  - split mutations into common vs rare pools with upgradable commons
  - changed elite room-end rewards to a single rare pick per player
  - added the `Spitter` enemy role
  - added room modifier data, map assignment, map display, and encounter-builder modifier injection
  - added runtime modifier handling for `Accelerating Waves`, `Enemy Speed`, `Spitter Swarm`, `Fire Floor`, `Ice Zone`, and `Mine Field`
  - added elite mini-boss spawns: `Elite Charger`, `Elite Spitter`, `Elite Support`
  - added the `hold_zone` side objective and room-limited temporary buffs
  - added reversible player and enemy modifier layers for ice-zone, aura, and temp-buff stacking
- Added a sustain tuning pass on top of the roadmap implementation:
  - increased base player move speed by `25%` (`390` -> `487.5`)
  - reduced base `Spitter` fire cadence to `1.0s`
  - added non-boss healing drops: `8%` chance to drop a `5 HP` pickup
- Updated `docs/development/current-state.md` so it reflects the post-implementation branch state instead of the earlier design-phase target state.

## Why

- `current-state.md` is the source of truth for the active runtime, but `start-of-day.md` had drifted far enough that it described a different game state.
- The branch/runtime naming had also become ambiguous because the live branch is still `v2/core-refactor` even though the runtime uses later design-direction changes.
- The two `.xlsx` design workbooks still describe older branch states:
  - one references the old `codex/primary-ruleset-plan` combat/ruleset branch
  - one references the older modifier/layout/recipe-heavy encounter runtime
- Continuation docs now point future work back to the actual branch and runtime that are live today.

## Runtime Impact

- Active gameplay behavior changed substantially on `v2/core-refactor`.
- The current branch runtime now includes:
  - common/rare mutation rarity flow
  - elite rare rewards
  - spitter enemies
  - healing pickups
  - room modifiers
  - elite mini-bosses
  - hold-zone buff objectives
- Headless parse validation was run after each major implementation batch and passed on the final branch state.
