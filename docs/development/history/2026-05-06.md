# 2026-05-06

## Summary

- Replaced the old two-choice run-flow UI with a connected node-map screen.
- Refactored `RunState` from a simple step/options list into a graph-backed row map with:
  - explicit node ids
  - row and column metadata
  - per-node `next_node_ids`
  - tracked `current_node_id`, visited nodes, and reachable nodes
- Changed normal runs to generate:
  - `3` starting nodes
  - `2–4` nodes on each non-boss row
  - one boss node on the final row
  - only same-column or adjacent-column links into the next row
- Preserved the existing room payload contract for combat, elite, rest, shop, boss, rewards, layouts, and boss setup so `GameWorld` did not need a behavioral rewrite.
- Rebuilt `RunFlow` to render:
  - connection lines
  - one button per node
  - route-state coloring for reachable, visited, current, locked, and boss nodes
  - a detail panel for room info on hover/focus
- Kept debug single-room mode working as a simplified one-node path instead of forcing it through the full connected map.
- Updated development and architecture docs to reflect the new connected-map model.

## Validation

- Headless Godot parse passed with:
  - `D:\GameDev\Godot_v4.6.2-stable_win64.exe\Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick --quit`

## Remaining Follow-Up

- Interactive validation is still needed for:
  - connected-map readability
  - route feel across multiple seeds
  - reachable/locked/current node clarity
  - rest/shop/boss placement comprehension
- Player 1 sprite follow-up also landed after the map slice:
  - added `player_p1_standing.png` and `player_p1_running.png` under `assets/sprites/player/`
  - removed the white background from both copied assets
  - swapped player 1 between standing and running sprites at runtime while keeping the existing hitbox
- Follow-up polish after that first sprite pass:
  - added `player_p1_running_alt.png` as a second running frame under `assets/sprites/player/`
  - removed the white background from the alternate running frame too
  - alternated the two running sprites at runtime while movement is active
- Bootstrap and run-state follow-up also landed:
  - added `Normal` and `Easy` run modes to the bootstrap flow
  - `Normal` keeps HP persistent between cleared rooms
  - `Easy` fully restores all players after each cleared room
- Follow-up player presentation pass also landed:
  - enlarged the player footprint by roughly `33%`
  - added `player_rifle.png` under `assets/sprites/weapons/`
  - attached the rifle sprite to player 1's aim pivot so it rotates with aim and mirrors on left aim
  - added `player_bullet.png` under `assets/sprites/weapons/`
  - switched player projectiles to the new bullet sprite while leaving enemy shots on their existing placeholder shape
- Combat reliability follow-up also landed:
  - fixed chaser contact damage so the red triangle uses a collision-aware damage threshold
  - fixed pickup spawns to attach through a deferred path instead of mutating `Area2D` state during physics query flush
  - cleaned a batch of strict GDScript warnings triggered by recent UI and sprite changes
- Cleanup and front-end follow-up also landed:
  - added a player-facing front menu with separate `Play`, `Meta`, `Settings`, and `Debug` entries
  - moved the setup-heavy room/loadout launcher behind the `Debug` path instead of exposing it first
  - kept `Normal` / `Easy` run-mode selection in the real play setup flow
  - retitled the route screen to player-facing wording (`Choose Route`, `Path Preview`) instead of the earlier dev-flavored labels
  - added a shared-gold HUD panel and tightened objective strings for survival, generator, and boss rooms
- Large progression rework also landed after that pass:
  - moved weapon profiles into `data/weapons.json`
  - split passive definitions into `data/passives.json`
  - replaced the old shared build state with per-player inventories and personal gold wallets
  - added two primary slots and two secondary slots per player, with live weapon switching
  - added physical loot drops after combat/elite room clears
  - added Take / Scrap voting, contested winner rolls, and duplicate-weapon leveling
  - added weapon replacement UI when a new weapon collides with full matching slots
  - replaced the old text-only combat status with per-player inventory HUD panels
  - changed room clear flow so loot resolves first, then an exit zone opens
  - replaced placeholder shop resolution with real shop rooms, personal offers, and ready-up before exit
- Cleanup pass after the progression rework also landed:
  - removed the dead shared-choice panel code path from `RunFlow`
  - removed old hidden status-label plumbing from `CoopManager`
  - updated room/node labels away from the old shared-upgrade/shared-shop wording
  - synced README and process docs to the current per-player loot/shop model
- HUD/input/weapon follow-up also landed after that cleanup:
  - fixed the loot-vote open/close bug by ignoring the button press that opened the vote UI
  - made the per-player inventory HUD more transparent so the arena remains readable underneath
  - added a passive-items strip to each player HUD card as placeholder equipment feedback
  - added `player_scattergun.png` and `player_slug.png` under `assets/sprites/weapons/`
  - removed the backgrounds from both new weapon sprites and wired them into the same attached-weapon path as the rifle
  - fixed weapon-sprite initialization order so scatter and slug now appear correctly after loadout application
  - moved gamepad dash to `B / O`
  - enlarged visible player and primary-weapon sprites by another roughly `33%`
  - increased the player collision radius to match the larger presentation
  - globally reduced primary fire intervals and secondary cooldowns by `20%` for a faster player combat pace
