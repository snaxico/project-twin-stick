# 2026-07-14

## V4 Playtest Findings Patch

- Tightened Upgrade eligibility to exact equipped abilities and explicit projectile/area/duration capability.
  Removed the retired Blink Twin Charge entry and corrected renamed ability requirements.
- Added the pause Encyclopedia button to the complete controller focus loop.
- Reduced Tank overshield to a `0.16` HP-fraction cap with `13/s` decay. Orbit now expires after a scalable
  `20s` lifetime.
- Strengthened next-room card focus/hover presentation and removed the ASCII trait glyph.
- Reworked ability feel:
  - Shockwave Resonance schedules one `0.5s` delayed slam at `1.5x` radius and `1.3x` damage.
  - Shockwave Dash deals `45` damage.
  - Afterburn follows the player and emits `70px` burning segments every `0.15s`; segments last `1.5s`.
- Rebuilt the WHERE catalog:
  - merged Fire/Frost/Mine grids and Islands into three skinned routes backed by one moving-safe-zone Hazard Floor
  - replaced Pinwheel with three roaming, players-only Sawblades that preserve a 120px dodge lane
  - replaced Pop-up Pillars with three irregular nine-pillar layouts, three-state warnings, connectivity-gated
    cover, and players-only rise damage
  - removed Sliding Gates, Bulwark, and Shifting Maze from scripts, pools, activation, labels, and profiling

## Validation

- Headless parse/boot and explicit `Bootstrap.tscn` smoke boot: pass after catalog cleanup.
- WHERE smoke passes:
  - Fire Hazard Floor: `103.0` avg FPS
  - Frost Hazard Floor: `105.2` avg FPS
  - Mine Hazard Floor: `96.9` avg FPS
  - Roaming Sawblades: `95.9` avg FPS
  - Pop-up Pillars: `70.8` avg FPS with the 200-enemy direct-injection cover probe
- `entity_ramp` 200 enemies + 200 projectiles: `138.3` avg FPS.
- `flowfield_stress` 200+200 varied substantially across consecutive runs: `31.9`, then `54.9` avg FPS.
  Arena-mechanic code is not active in this isolated harness; repeat in a quiet session before retuning.
- Runtime/data stale-reference scan for Islands, the merged grid scripts, Sliding Gates, Bulwark, and Shifting
  Maze: clean.

## Remaining Live Checks

- Focused 1P/2P feel test for the redesigned WHERE mechanics, Tank/Orbit balance, route-card selection, and
  Shockwave/Afterburn feel.
- `vampiric_wake.wake_lifesteal` has no runtime consumer. The approved plan does not define ownership or
  rounding, so it remains visible and unimplemented pending a design decision.
