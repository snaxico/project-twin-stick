# 2026-07-14

## V4 Playtest Findings Patch

- Tightened Upgrade eligibility to exact equipped abilities and explicit projectile/area/duration capability.
  Removed the retired Blink Twin Charge entry and corrected renamed ability requirements.
- Added the pause Encyclopedia button to the complete controller focus loop.
- Reduced Tank overshield to a `0.16` HP-fraction cap with `13/s` decay. Orbit now expires after a scalable
  `20s` lifetime.
- Strengthened next-room card focus/hover presentation and removed the ASCII trait glyph.
- Reworked ability feel:
  - Shockwave Resonance schedules one `0.5s` delayed slam at `1.5x` radius and `1.3x` damage.
  - Shockwave Dash deals `45` damage.
  - Afterburn follows the player and emits `70px` burning segments every `0.15s`; segments last `1.5s`.
- Rebuilt the WHERE catalog:
  - merged Fire/Frost/Mine grids and Islands into three skinned routes backed by one moving-safe-zone Hazard Floor
  - replaced Pinwheel with three roaming, players-only Sawblades that preserve a 120px dodge lane
  - replaced Pop-up Pillars with three irregular nine-pillar layouts, three-state warnings, connectivity-gated
    cover, and players-only rise damage
  - removed Sliding Gates, Bulwark, and Shifting Maze from scripts, pools, activation, labels, and profiling

## Validation

- Headless parse/boot and explicit `Bootstrap.tscn` smoke boot: pass after catalog cleanup.
- WHERE smoke passes:
  - Fire Hazard Floor: `103.0` avg FPS
  - Frost Hazard Floor: `105.2` avg FPS
  - Mine Hazard Floor: `96.9` avg FPS
  - Roaming Sawblades: `95.9` avg FPS
  - Pop-up Pillars: `70.8` avg FPS with the 200-enemy direct-injection cover probe
- `entity_ramp` 200 enemies + 200 projectiles: `138.3` avg FPS.
- `flowfield_stress` 200+200 varied substantially across consecutive runs: `31.9`, then `54.9` avg FPS.
  Arena-mechanic code is not active in this isolated harness; repeat in a quiet session before retuning.
- Runtime/data stale-reference scan for Islands, the merged grid scripts, Sliding Gates, Bulwark, and Shifting
  Maze: clean.
- Follow-up warning hygiene renamed the Enemy RNG seed parameter, replaced WaveDirector's mixed-type spawn-source
  ternary with an explicit helper, and disambiguated FlowField's ring-search locals. Headless parse and the
  explicit Bootstrap smoke boot both pass, and generated script UIDs are tracked for the new arena mechanics
  and Afterburn wake.

## Pre-follow-up Open Items

- Focused 1P/2P feel test for the redesigned WHERE mechanics, Tank/Orbit balance, route-card selection, and
  Shockwave/Afterburn feel.
- Vampiric Wake ownership/capping was not defined by the first patch; the follow-up below closes that item.

## Arena, Tank, and Vampiric Wake Follow-up

- Added three hidden deterministic layouts to every non-open WHERE, shared no-repeat history for the Hazard
  Floor family, stored route seeds/layouts for retry stability, and normalized hazards to players-only damage.
- Added Sweeping Laser Lanes to Horde, Mixed, Splitters, and Gauntlet with a `1.0s` warning, `3.2s` constant
  sweep, `1.2s` rest, and fixed `420px` co-op opening. Unified warning/safe/cover/hazard colors.
- Added two Hazard Floor families, asymmetric variants for existing mechanics, and transactional Drifting Cover
  moves. All `30` WHERE ID/layout PerfRunner smokes pass.
- Reduced Tank sustain to `1` heal per kill, Gorge `+2`, `12%` overshield, `15/s` decay, and Blood Frenzy `15`.
  Fixed exact-fill heals creating overshield and made the HUD scale against the real cap.
- Implemented Vampiric Wake as `1 HP` per real-damage hit with one player-owned `4 HP/s` bucket shared across
  casts and segments. Full-health/immune/zero-damage hits consume nothing and Wake never grants overshield.
- Made the isolated harness deterministic and added 160. Entity retention passed (`99.3` pre median, `93.4`
  post median), but the required flow gate failed: pre `55.2/53.1/57.5`, post `53.7/50.3/51.3` FPS. The cover
  cap remains `160`; broader optimization and cap changes were deliberately not attempted.

## Follow-up Live Checks

- Playtest all three WHERE layouts in 1P/2P, prioritizing laser readability, separated-player chip damage,
  Drifting Cover transitions, Tank mortality, and Vampiric Wake feel.
- The flow-field 160 acceptance gate remains open and requires a separate performance plan.
