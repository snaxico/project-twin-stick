# 2026-05-18

## Summary

- Converted the active branch to the then-current v2 core-refactor runtime plan.
- Rewrote the live combat/run loop around a bigger open arena, zoom camera, single primary/secondary loadout, and per-player mutation progression.
- Archived the obsolete v1-only systems into a separate repo area during the transition.

## Outcome

- Live branch runtime now centers on:
  - `1–2` players
  - `Rifle` + `Grenade`
  - `Chaser`, `Charger`, and `Boss`
  - `survive`, `capture_the_hill`, `rest`, and `boss` flow
  - per-player mutation picks after room clears
- Encounter builder now supports:
  - room type
  - objective
  - depth
  - enemy mix override
  - starting mutation presets
- Old v1 systems are no longer part of the live scripts/scenes/data tree.

## Verification

- Headless Godot parse check passed after the refactor:
  - `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\\GameDev\\Project_Twin_stick --quit`
- Grepped the live `scripts/`, `scenes/`, and `data/` trees for archived v1 system names after the move.
- Remaining references to retired systems are limited to docs/history/archive context.

## Follow-Up

- Main remaining work is gameplay validation, not more structural refactor:
  - camera feel
  - wave pacing
  - hold-zone pacing
  - mutation impact / stacking feel
  - boss tuning
- `3–4` player support is intentionally deferred until the `1–2` player v2 loop validates well.
