# 2026-05-05

## Summary

- Applied a balance-and-readability follow-up after gameplay feedback while keeping the approved core loop intact.
- Reduced enemy ranged pressure and softened the most oppressive firing patterns instead of expanding combat scope.
- Improved readability with stronger aim-line contrast, clearer projectile silhouettes, and a darker arena backdrop behind combat reads.
- Replaced the mixed secondary actor path with a clean split between thrown grenades and proximity mines.
- Added a proximity-mine family with instant placement on secondary press and a doubled proximity trigger radius.
- Restored the original grenade family as usable secondary choices in parallel with mines.
- Added a full identity/content pass without adding custom art or new core systems.
- Rebuilt enemy archetype silhouettes, hitbox sizing, and bob timing so each archetype reads distinctly.
- Added layout-specific palettes and floor landmarks so arena presets look intentionally different.
- Replaced the old debug combat HUD with stacked player bars, a modifier chip, a timer bar, and styled panels.
- Expanded the item pool from `11` to `19` entries and added `move_speed_mult` item support.
- Expanded tactical modifiers with `Darkness`, `One-Way`, and `Friendly Fire`.
- Replaced the fixed run structure with procedural `5–7` step node-map generation plus validation fallback.
- Added depth-aware wave composition, depth-scaled combat rewards/timers/spawn intervals, and modest boss HP scaling by rooms survived.
- Added real aim settings UI in both bootstrap and pause flow so per-player aim mode no longer depends on debug-only controls.
- Synced the documentation to the actual runtime state and current design rules.

## Notes

- The shared secondary family is now six variants total: `Grenade`, `Cluster Grenade`, `Siege Grenade`, `Mine`, `Shrapnel Mine`, and `Heavy Mine`.
- Mines now use their own scene/script path; grenades use their own scene/script path again.
- Runtime compatibility was preserved for older secondary IDs where practical so debug/setup state does not silently break.
- Headless Godot validation passed with the local console executable under `D:\GameDev\Godot_v4.6.2-stable_win64.exe\`.
- Startup validation also passed after the HUD/layout/modifier pass.
- The run map contract did not change: `RunFlow` and room configuration still consume the same node dictionary structure.
- A single-player out-of-bounds bug in the new pause settings flow was fixed with additional player-slot guards before push.
- Follow-up cleanup removed a dead `RunState._ready()` RNG randomize, added a support-slot fallback guard in procedural map generation, and replaced the fixed six-entry wave composition buffer with a size derived from the requested spawn count.
- Remaining work is tuning and play validation, not system scaffolding.
