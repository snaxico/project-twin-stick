# 2026-06-03

## Summary

- Implemented the playtest round-4 plan locally on `v3/main`.
- Implemented the playtest round-5 follow-up patch on top of the round-4 patch stack.
- Fixed unused-parameter warnings from the Warden behavior and homing-orb helper.
- Updated docs for the manual playtest handoff and marked this as the newest stable baseline.

## What Changed

- Removed level-up VFX and its runtime listener.
- Tuned baseline and abilities:
  - starter `Rifle` fire rate is now `5.0`
  - `Overcharge` is now `1.5x` fire-rate plus `2x` projectiles
  - `Blink` now uses movement direction, `340px` range, short arrival i-frames, and arrival detonation
  - `Turret` and `Minefield` were buffed
  - mines now separate trigger radius from explosion radius
- Reworked Fire Trail:
  - longer / faster ticking path zones
  - larger usable path-zone radius
  - one larger impact pool on hit / expiry
  - new impact-pool params compile through `MutationSystem.gd`
- Increased Fire Floor pressure:
  - radius `280`
  - max active zones `7`
  - faster spawn interval
  - margin adjusted so larger zones stay inside the arena
- Reworked HUD / pause clarity:
  - dedicated side-objective panel with label, progress text, and progress bar
  - Kill Streak is now a true fixed-target streak and resets on actual player damage
  - pause `Settings` now opens the existing in-run panel
  - in-run settings control screen effects and per-player aim mode
- Reworked elite / boss pressure:
  - elites spawn away from player center spawn positions
  - Elite Charger has a stronger telegraphed charge and slam
  - Elite Spitter uses a wider led projectile fan
  - Elite Support adds a damaging pulse to its minion pressure
  - bosses have higher threat, HP-threshold phase telegraphs, add pressure, and heavy-attack tells
- Added performance cheap wins:
  - player and enemy projectiles now use a reusable pool
  - enemy target acquisition is staggered across physics frames
  - inactive pooled projectiles are ignored by shockwave / VFX load checks
- Removed dead `_estimate_room_total_enemies()` after Kill Streak switched to fixed target.

## Validation

- Headless parse passed:
  - `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick --quit`
- `git diff --check` passed with only line-ending warnings from Git.

## Important Notes

- Phase 0 profiling from the plan was not executed because the plan did not define a reproducible stress scenario or profiler export path. The performance implementation itself was completed.
- Objective-panel icons currently use simple letters (`H` / `K` / `C`). This is acceptable for the current build but may need a later IconFactory/drawn-glyph polish pass.
- `docs/development/playtest-round-4-plan.md` is still untracked unless it gets added before commit.

## Tomorrow Playtest Focus

This section was the round-4 handoff and is now superseded by **Current Playtest Focus**
below. Keep it as historical context only.

- Validate `1P` and `2P` room pressure with the `5.0` fire-rate rifle.
- Check Blink direction, landing safety, and detonation usefulness.
- Check Fire Trail and Fire Floor balance.
- Stress `100-150+` enemies/projectiles and watch for stutter after pooling / AI time-slicing.
- Test elite spawn distance and new elite patterns.
- Test boss phase transitions, adds, heavy attacks, and Hydra difficulty.
- Test pause Settings with keyboard/controller navigation, screen effects, and per-player aim mode.
- Confirm Kill Streak resets only when actual damage is taken.

## Round-5 Follow-Up Patch

- Implemented `docs/development/playtest-round-5-plan.md` on top of the round-4 patch stack.
- Treat the combined round-4 + round-5 build as the newest stable runtime for manual playtest and future work.

## Round-5 What Changed

- Removed the in-run pause Settings panel:
  - main-menu Settings / input rebinding remains intact
  - pause menu now shows Resume / Restart Room / Main Menu plus build summary
  - aim mode support remains in code but current runtime defaults to auto-target
- Reworked Fire Trail into `Fire Bullets`:
  - no along-flight fire zones
  - projectiles create one smaller burning impact pool on hit / expiry
  - params are `50px`, `2.0s`, `0.4` damage percent
- Nerfed `Overcharge`:
  - cooldown `22s`
  - duration `4s`
  - `1.3x` fire-rate
  - no extra projectiles
- Added additive ability lockout support:
  - `Player.apply_ability_lockout(seconds)`
  - `Dash.extend_cooldown(seconds, now)`
  - Pulsar EMP delays Dash and non-Dash ability cooldowns
- Increased boss HP to round-5 values:
  - Warden `1650`
  - Hydra `2100`
  - Hive `1800`
  - Pulsar `1950`
- Reworked bosses:
  - Warden: leap, multi-charge, ground-pound shockwave, speed ramp
  - Hydra: aimed snipes, rotating sweep, phase minions, slow homing orbs
  - Hive: shield minions, poison cloud, burrow relocate, escalating minion mix
  - Pulsar: teleport, EMP, sweeping beam, stronger phase-3 pressure
- Added review safeguard after implementation:
  - Hive shield count fixed at `4`
  - Hive phase transitions only spawn a new shield if the old shield has already been cleared
- Reworked elites:
  - Act 2 elites get `1.5x` HP, `1.3x` damage, and `25%` faster elite cooldowns
  - elite rooms spawn 2-3 regular adds every `6-8s` while the elite is alive

## Round-5 Validation

- Headless parse passed:
  - `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick --quit`
- Follow-up headless parse after unused-parameter warning cleanup also passed.
- `git diff --check` passed with only line-ending warnings from Git.

## Current Playtest Focus

- Use `docs/development/playtest-round-5-validation.md` as the active checklist.
- Validate the combined round-4 + round-5 build, not the old round-4-only checklist.
- Watch especially:
  - Warden reach and telegraph readability
  - Hydra sweep / snipes / homing orbs
  - Hive shield, burrow, poison, and whether the fight becomes a slog
  - Pulsar teleport / EMP / beam readability
  - Act 2 elite scaling and add-wave pressure
  - Fire Bullets impact-pool balance and performance
  - boss entity-load performance in deep endless / late Act 2
  - main-menu binding editor still works after removing in-run Settings

## Round-6 Performance / Gameplay Patch

- Implemented `docs/development/playtest-round-6-plan.md` on top of the round-4 + round-5 baseline.
- Changed `Mine Field` display to `Scanline` while keeping id `mine_field`:
  - scanlines now draw segmented sweep lines with 2-3 safe gaps
  - removed per-mine circle/arc draw spam from the sweep
- Changed `Duration` mutation behavior:
  - sustained abilities scale
  - instant/movement abilities (`Blink`, `Dash`) do not scale
- Converted Fire Bullets impact pools:
  - `FireTrailZone` is now `Node2D`
  - damage is team-routed through distance checks instead of `Area2D` overlap tracking
- Reduced enemy visual draw cost:
  - removed enemy `Shadow` and `Outline` polygon nodes
  - split static visual refresh from per-frame facing/fuse visual updates
- Generalized Hive shield internals into boss deflector state.
- Added Pulsar reactive close-range teleport.
- Initially added debug-only boss add-wave dry-run support:
  - enabled only with `debug_boss_add_waves`
  - normal runs do not ship heavy boss add-waves yet
  - cap counts live non-boss enemies plus pending spawns in boss rooms
- Added committed dev profiling harness under `scenes/dev/` and `scripts/dev/`.

## Round-6 Validation

- Headless parse passed after implementation:
  - `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick --quit`
- Follow-up warning cleanup removed Godot local-name warnings in:
  - `scripts/weapons/FireTrailZone.gd`
  - `scripts/modifiers/MineFieldModifier.gd`
- Non-headless profiling harness passed:
  - `50+50`: `1174.6 FPS`, `154` draw calls
  - `100+100`: `461.3 FPS`, `316` draw calls
  - `150+150`: `191.9 FPS`, `487` draw calls
  - `200+200`: `73.8 FPS`, `651` draw calls
- Headless harness run also completed, but draw calls were `0`; do not use headless harness output for render profiling.
- Live playtest feedback after the patch reported a massive performance improvement.
- Treat this round-6 patch as the newest stable baseline for future work.

## Round-6 Open Validation

- Real boss-room stress validation still must be run manually now that capped heavy boss add-waves are enabled in normal runs.
- Verify Fire Bullets pools still damage enemies correctly and do not friendly-fire.
- Verify enemy readability after removing Shadow/Outline nodes.
- Verify Scanline gap readability and damage fairness.

## Round-6 Boss Add-Wave Promotion

- Promoted capped boss add-waves from debug-only to normal boss-room runtime.
- Removed the `debug_boss_add_waves` runtime gate and stale debug setup passthrough.
- Boss add-wave budget still caps all non-boss boss-room enemies, including pending/deferred spawns, at `25`.
- Warden, Hydra, and Pulsar receive periodic add-wave pressure; Hive keeps native minion/deflector behavior under the same budget.
- Remaining validation/tuning is not an implementation blocker for B2.
- B3 Pulsar deflector-spawned adds remain unimplemented until the desired Pulsar deflector count/pattern is specified.

## Runtime VSync Setting

- Added a main-menu Settings VSync toggle.
- VSync defaults on in `project.godot` and when no saved video setting exists.
- The VSync choice is persisted separately from input bindings in `user://video_settings.cfg`.
- The profiling harness still disables VSync explicitly so benchmark FPS is not capped.

## Round-7 Spectacle / Ability Slots / Mutation Patch

- Implemented `docs/development/playtest-round-7-plan.md` in the required order.
- Track B:
  - added `slot: off|def` to all abilities
  - pre-run ability selection is now split into `LT / OFF` and `RT / DEF`
  - loadout defaults and old selections normalize to `1 OFF + 1 DEF`
  - fixed gamepad fallback mapping so slot 1 uses LT and slot 2 uses RT
- Track A1-A7:
  - enabled HDR 2D and added `WorldEnvironment` glow
  - added `HitStopManager.gd` as the single `Engine.time_scale` owner
  - added boss HP bar with phase pips
  - added level-up flash + procedural sting without mid-combat slow-mo
  - strengthened shake, muzzle flash, death particles, projectile trails, and procedural SFX
- Track C:
  - added required projectile visual/SFX fields to behavior-changing weapon mutations
  - `Projectile.gd` now consumes projectile shape, trail style, accent color, and impact SFX
  - player projectile core fill stays player tint for readability
- Track D pilot only:
  - added loadout-gated `oc_piercing_overdrive` for Overcharge
  - added loadout-gated `sw_resonance` for Shockwave
  - ability rare effects compile through `MutationSystem.gd` into runtime ability stats
  - Overcharge rare changes `pierce_count` and projectile speed in `Player.gd`
  - Shockwave rare schedules extra pulses in `CoopManager.gd`
- A8 fire-rate bump remains deferred by plan.

## Round-7 Validation

- Headless parse passed:
  - `D:\GameDev\Godot_v4.6.2-stable_win64.exe\Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick --quit`
- `git diff --check` passed with only line-ending warnings from Git.
- Follow-up warning cleanup fixed the repeated `scene_tree` local declaration in `FireTrailZone.gd`.

## Round-7 Open Validation

- Manual `1P` and `2P` playtest still needs to validate:
  - OFF/DEF picker usability and `LT = OFF` / `RT = DEF` runtime mapping
  - bloom readability and performance with DebugOverlay F3
  - hit-stop feel and no trash-kill stutter
  - boss HP bar readability
  - distinct mutation projectile visuals/SFX
  - `oc_piercing_overdrive` and `sw_resonance` behavior
  - boss add-wave performance with the round-7 visuals enabled

## Pulsar Beam Removal

- Removed the Pulsar sweeping beam attack entirely — reported as unfair (no beam-path
  telegraph; the pre-fire ring only signalled "something", and damage started on frame 1) and a
  perf cost (a new `Line2D` + tween spawned every `0.08s` during each beam).
- `Enemy.gd`: removed `_pulsar_beam_at` (declaration, reset, init, and the beam trigger in
  `_update_pulsar_behavior`).
- `CoopManager.gd`: removed `_active_beams` (var + clear + tick call) and the
  `spawn_pulsar_beam` / `_update_active_beams` / `_draw_beam_line` functions.
- Pulsar still has teleport (incl. reactive), EMP pulse, shockwave + hazard zones, minion
  spawns, aimed projectile fans, and the deflector — so the fight stays complete.
- Headless parse passed after removal.
