# 2026-07-06

## V4 polish round implemented

Implemented `docs/development/v4-polish-round-plan.md` in the
`D:\GameDev\Project_Twin_stick_v4` worktree on branch `v4/class-system`.

### What changed

- Split enemy movement targeting from contact-damage targeting:
  - players are in the `player` group for aggro
  - deployables remain damageable through `player_deployable`
  - deployables no longer join `player_target` or divert enemy movement
- Remapped ability slots to `A / X / B / Y`, with `Y` as the locked ultimate slot, across input map,
  options labels, HUD labels, pause build overlay, and loadout summary text.
- Reworked the loadout ability picker into explicit `A`, `X`, and `B` slot targets plus a locked `Y`
  ultimate card; ability selection no longer silently replaces the oldest chosen ability.
- Added `ClassVisuals.gd` as the shared class identity table and consumed it in loadout/HUD styling and
  player arena silhouettes.
- Added abstract-geometric class silhouettes and class-accented movement trails for Mobile, Tank,
  Controller, and Risk.
- Expanded weapon/ability/ultimate VFX using existing pooled/suppressed effect paths:
  - Whirlwind crescent sweep
  - Flamethrower cone polygon
  - Arc Wand jagged lightning
  - stronger cast pulses, summon materialize flashes, reinforce glints, Ignite embers, and contained ultimate
    accents
- Fully removed retired runtime content:
  - weapon data for cannon, railgun, boomerang
  - duplicate weapon-data Shockwave entry
  - ability data/runtime paths for Blink and Decoy
  - Decoy node and taunt targeting path
  - boomerang projectile return behavior
- Refreshed Encyclopedia categories to include classes, class pools, abilities/ultimates, upgrades, modifiers,
  enemies, and system rules from live data where possible.
- Applied the locked balance pass:
  - lower trash HP and contact damage
  - higher density through spawn-count scaling and slightly faster current-code spawn intervals
  - flattened weapon level curves
  - ability/deployable tuning
  - softened Risk Overheat vulnerability
  - reduced Bloodthirst sustain for higher-density rooms
  - reduced combat ultimate charge rates
  - completed Slipstream with damage boost, enemy/projectile slow, and dash recharge

### Validation

- JSON parse:
  - `data/weapons.json`
  - `data/abilities.json`
  - `data/mutations.json`
- `git diff --check`
- Headless parse:
  `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick_v4 --quit`
- Bootstrap smoke boot:
  `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick_v4 res://scenes/ui/Bootstrap.tscn --quit`
- PerfRunner:
  `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick_v4 -- --profile=champion:hive --players=2 --build=heavy`
  - Result: `avg_fps=145.0`, `min_fps=144.0`, `max_frame_ms=6.944`.
- Post-review PerfRunner rerun for the density/VFX check:
  - Result: `avg_fps=144.9`, `min_fps=144.0`, `max_frame_ms=6.944`.

### Next

- Live 1P/2P feel check across all four classes.
- Verify contact-damage cadence, ultimate cadence, loadout assignment ergonomics, and dense-room readability.
