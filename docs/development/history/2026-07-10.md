# 2026-07-10

## V4 Room Redesign Implementation

- Implemented the canonical room model from `docs/development/v4-room-redesign-MASTER.md`:
  - WHO compositions in `data/room_archetypes.json`
  - one gated WHERE mechanic per room
  - 0-1 existing TWIST modifier per room
- Added telegraphed spitter fans and WaveDirector shooter-budget reservation/release.
- Added all planned WHERE mechanic scripts and CoopManager activation:
  - pulsing Fire/Frost/Mine grids
  - Islands, Pinwheel, Tesla Arcs, Drifting Clouds
  - Bastion, Pop-up Pillars, Sliding Gates, Bulwark, Drifting Cover, Shifting Maze
- Added CoopManager-owned obstacle reconciliation for cover mechanics, including connectivity checks and
  relocation for stranded enemies, pickups, and player deployables.
- Updated FlowField with the planned safe changes and bounded failure instrumentation.
- Extended PerfRunner with `--profile=shooter_budget --smoke` and `--profile=where:<id> --smoke`.

## Validation

- Parse: pass.
- `--profile=shooter_budget --smoke`: pass.
- `--profile=entity_ramp`: pass.
- WHERE smokes: pass for Fire Grid, Frost Grid, Mine Grid, Islands, Pinwheel, Tesla Arcs, Drifting Clouds,
  Bastion, Pop-up Pillars, Sliding Gates, Bulwark, Drifting Cover, and Shifting Maze.
- `--profile=flowfield_stress`: initial Phase 4 run was still below target and reported `update_targets` as
  the dominant bucket at 200 enemies.

## V4 Room Redesign Cleanup

- Added Pop-up Pillars transition warnings while preserving the planned 1.8s up / 1.2s down duty cycle.
- Updated obstacle reconciliation to reuse unchanged and same-size obstacle bodies, freeing only removed or
  resized bodies.
- Revalidated parse plus `where:popup_pillars --smoke` and `where:drifting_cover --smoke`.

## FlowField Phase 4b

- Replaced per-cell nearest-reachable ring search with reverse-BFS escape-vector generation.
- Added per-player target-field rebuild throttling.
- Coarsened flow-field cells from `100px` to `150px` in runtime and profiling.
- Revalidated parse and `flowfield_stress`: latest 200-enemy step reached `59.4` avg FPS, with instrumentation
  showing physics as the dominant remaining bucket (`update_targets_ms 3.831`, `sample_ms 0.200`).
