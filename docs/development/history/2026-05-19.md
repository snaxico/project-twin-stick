# 2026-05-19

## Summary

- Reviewed the v2 refactor branch after feedback that the gameplay result felt like a step back.
- Verified the flagged technical points around:
  - grenade charge behavior
  - dash-damage wiring
  - builder row reuse
- Applied gameplay/readability follow-up fixes and documented the branch changes in more detail.

## Outcome

- Confirmed the following systems were already correctly wired:
  - grenade extra-charge behavior uses one shared cooldown after the last charge is spent
  - builder `Enemy Mix` and `Starting Mutations` rows intentionally reuse old hidden builder rows
- Fixed buffered dash damage so it now follows the actual dash direction rather than aim direction.
- Improved arena readability with:
  - brighter floor grid lines
  - stronger repeating major guide lines
  - visible perimeter wall visuals
- Increased room pressure by:
  - lowering initial spawn delay
  - increasing spawn cadence
  - increasing live enemy cap
  - increasing per-wave spawn count
- Fixed survive rooms so they now end immediately when the survival timer reaches `0.0`.

## Documentation Added

- Added `docs/development/v2-refactor-summary.md` as a consolidated overview of the branch changes.
- Updated `docs/development/current-state.md` to reflect the latest arena/readability/pacing state.

## Verification

- Headless Godot parse check passed after the gameplay follow-up fixes.

## Follow-Up

- The branch is now better documented and technically cleaner, but the main issue is still gameplay acceptance, not code correctness.
- If the v2 direction is reconsidered, this branch should be treated as a reusable parts bin rather than assumed to be the final direction.

## V3 Implementation

- Converted the live branch from the earlier v2 control model to the current auto-fire / shockwave / dash loop.
- Replaced manual primary fire with auto-attack:
  - `Player.gd` now tracks separate movement, auto-attack, and shockwave directions
  - `AutoTarget.gd` replaced the old aim-assist runtime for target acquisition
  - rifle baseline cadence is now `3.0` shots per second
- Replaced grenade with shockwave:
  - cooldown now lives in `Player.gd`
  - `CoopManager.gd` now resolves the blast, knockback, ring visual, and screen shake
  - secondary mutations were remapped to `shockwave_radius` and `shockwave_cooldown`
- Updated presentation toward the v3 neon slice:
  - player body is now a chevron-only polygon
  - projectile visuals are now glowing orb polygons
  - arena grid and wall colors now shift by room depth
  - player default colors are now cyan / magenta
- Removed stale live references:
  - archived `GrenadeProjectile.tscn` and `GrenadeProjectile.gd`
  - archived the old `AimAssist.gd`
  - moved unused `data/unlocks.json` out of the live tree with meta progression still deferred

## V3 Verification

- Headless Godot parse check passed after the v3 implementation.
- Grep over live `scripts/`, `scenes/`, and `data/` shows no remaining grenade or `AimAssist` references.

## Control + Camera Follow-Up

- Removed the remaining settings surfaces for:
  - aim mode
  - screen effect level
- Locked full-screen effects on in the live runtime and profile state.
- Simplified default player assignment back to:
  - `P1` = gamepad
  - `P2` = keyboard
- Rewired keyboard combat input so the live keyboard player now uses:
  - `WASD` for movement
  - `Ctrl` for dash
  - `Space` for shockwave
- Removed the hard player-to-player separation clamp after it caused aggressive rubberbanding in `2P`.
- Kept same-screen co-op behavior on the shared camera:
  - players can move freely while they remain inside the current camera view
  - the camera zooms out to keep both players visible
- Hardened player spawn / restore placement around arena center so both players begin side-by-side in the middle of the arena instead of spawning into broken edge states.

## Latest Verification

- Headless Godot parse check passed after the control and camera follow-up changes.
