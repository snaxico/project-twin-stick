# 2026-07-20

## Round-1 Playtest + Round-2 Planning Session (no code changes)

- User played the implemented feel-polish patch (`c728aa0` + `9afac67`). Findings: performance still
  not fixed in live play ("we need a real solution"); classes play and balance very unevenly;
  kit-size question raised (2 abilities + ult vs current 3); summon Signature did nothing;
  Sweeping Laser Lanes gaps are fixed and should vary while alternating axes; Mobile reads as the
  game's default class.
- Root-caused without code changes: `legion` (`construct_count_bonus`) is dead data — the param is
  never read by runtime code. Laser gaps confirmed hardcoded in `SWEEP_SEQUENCES`. (An initial claim
  that `aegis` was equally dead was stale and corrected in plan review: `_reinforce_deployables`
  reads `shield_amount` and applies deployable shields.)
- Decisions (MC):
  - **Spawn-model verdict open** — both models stay; the Round-2 deciding playtest settles it.
  - Perf is felt in **summon-heavy Controller play, dense hordes anywhere, champion+wave moments**
    (not primarily cover rooms). **Structural work PRE-AUTHORIZED**: measure the three felt
    scenarios first, then the evidence-pointed structural fix proceeds within the patch.
  - **Kit size: prototype 2 vs 3** behind a debug Run-Setup option; playtest decides.
  - **Mobile = official baseline class**; others balanced around it (standing design principle).
  - **Upgrade fun-pass deferred to Phase 3**; this patch does the wiring audit + fixes only.
  - **5b weapon-identity juice included in full.**
- Wrote `docs/development/v4-feel-polish-round-2-plan.md` (6 slices: felt-scenario instrumentation →
  pre-authorized structural perf fix → laser variation + mutation wiring audit/fixes → weapon-identity
  juice → kit-size prototype → deciding playtest that closes spawn model, kit size, and perf, then
  gates the Mobile-baseline class-balance tuning plan). Updated start-of-day pointers and memory.
  **Nothing implemented.**

## Plan Review Round

- Review found: Slice 1 promised subsystem cost buckets the harness cannot produce (PerfRunner samples
  only aggregate engine monitors); the aegis dead-data claim was stale (it is wired); the Slice-4
  fire/impact weight table named only the single existing `_weapon_impact_weight` field.
- Plan corrected: Slice 1 now explicitly builds a `PerfProbe` timing collector (debug-profiling-gated
  begin/end buckets for enemy physics, deployable tick, projectile system, CombatEffects) before the
  felt scenarios count, with a probe-overhead check in the gate; aegis removed from the fix list
  (audit-only); Slice 4 adds a `_weapon_fire_weight` contract for the muzzle call site so both table
  columns land on real fields.

## Plan Review Round 2

- Review confirmed the previous three fixes and found two more: the kit-size prototype would be
  silently normalized back to 3 abilities (`RunState` pads to 3 at ~568; `Bootstrap` pads at ~1664
  and rejects non-size-3 at ~1844), and the `felt:*` scenarios used elastic terms unfit for an
  absolute min-FPS gate.
- Plan corrected: `debug_run_setup.kit_size` seam through all three normalization/validation call
  sites (2-mode kits are genuinely 2 abilities at runtime, not hidden UI); felt profiles fully
  pinned — exact classes/weapons/abilities/mutations, depths (8/10/10), live-count holds
  (100/200/150), modifier IDs (`enemy_speed`,`shielded`), Hive at `t=10.0s`, trickle model, 40s
  window with 5s warmup exclusion, 3 runs each.

## Plan Review Round 3

- Two P1s: `felt:summons` depended on post-Slice-3 Legion and misstated the summon cap as
  per-player (it is one shared array trimmed to `MAX_ACTIVE_SUMMONS = 5` total); removing the
  third ability slot in 2-mode would shift the ultimate off slot 4/`Y` because input is
  slot-index based.
- Fixed: Slice-1 baseline uses current code only (`overgrowth`, no `legion`; shared 5-cap stated),
  with a Slice-3 re-run of `felt:summons` after Legion lands; 2-mode keeps the four-slot structure
  with an explicit empty sentinel at slot 3 (no-op presses, hidden HUD, ignored by eligibility) so
  the ultimate stays at slot 4/`Y` with identical bindings in both A/B modes.

## Open

- Run the Slice-6 deciding playtest.
- After verdicts: remove the losing spawn model + kit size; draft and MC-lock the class-balance
  tuning plan (Mobile baseline).

## Round-2 Plan Implementation

- Implemented Slices 1-5 from `docs/development/v4-feel-polish-round-2-plan.md`.
- Added `PerfProbe` and `PerfRunner` felt profiles:
  - `felt:summons`: 2P Controller deployable profile, heavy Arc Wand, Overgrowth, 100 held enemies, trickle.
  - `felt:horde`: 1P Mobile horde profile, heavy Rifle, 200 held enemies, trickle.
  - `felt:champion_wave`: 2P Mobile+Tank, heavy, Hive at `t=10s`, 150 held enemies, trickle.
- Structural perf changes:
  - high-load open-room enemies can use soft crowd movement above the 120-enemy physics wall;
  - dense-cluster soft movement staggers non-champion expensive AI/contact refreshes while preserving
    per-frame movement;
  - same-frame nearby-enemy query cache in `CoopManager`;
  - deployable target/candidate scans are cached briefly per deployable;
  - `ParticleFactory` tags cosmetic-only transient nodes; runtime trim removes only tagged cosmetics,
    protecting gameplay zones such as FireTrailZone and AfterburnWake;
  - projectile/effect VFX suppression engages earlier under load while audio still plays.
- Playtest fixes:
  - Sweeping Laser Lanes lazily roll deterministic gap centers from route seed + variant while strictly
    alternating axes.
  - `legion.construct_count_bonus` is read at summon time and still respects shared `MAX_ACTIVE_SUMMONS`.
  - Weapon identity is wired: each weapon assigns fire profile/weight and impact weight; SFX profiles now
    cover Beam, Whirlwind, Arc Wand, and Flamethrower; projectile impact audio suppresses duplicate generic
    hit audio in the same physics frame; non-champion death cues use coarse weight groups; Dash no longer
    stacks generic activation explosion under its dash cue.
  - Debug Encounter Builder has a Kit Size row. 2-mode stores two abilities + empty slot-3 sentinel +
    ultimate in slot 4/Y; HUD hides the sentinel and Player ignores empty-slot presses.
- Validation:
  - Godot headless editor parse: PASS.
  - `FeelPolishAcceptance.tscn`: PASS (`total=75`, seeded trickle/pulsed repro green).
  - `where:sweeping_laser_lanes --smoke`: PASS.
  - `felt:summons`: PASS across 3 pinned heavy runs (`min_fps=141.0 / 136.0 / 135.0`).
  - `felt:horde`: PASS across 3 pinned heavy runs (`min_fps=144.0 / 140.0 / 139.0`).
  - `felt:champion_wave`: PASS across 3 pinned heavy runs (`min_fps=68.0 / 117.0 / 80.0`).
    Isolated high `max_frame_ms` samples remain during Hive activity, so Slice 6 still needs live-feel
    verification.
