# 2026-07-04

## V4 class-system Slice 1 implemented

Implemented Slice 1 of `docs/development/v4-implementation-plan.md` in the
`D:\GameDev\Project_Twin_stick_v4` worktree on branch `v4/class-system`.

### What changed
- Added class-aware inventory fields: `class_id`, `passive_id`, and `ultimate_id`.
- Expanded `ClassRegistry.gd` and `RunState.gd` with class catalogs, class-pool weapon / ability catalogs,
  and loadout normalization.
- `RunState.start_new_run` now accepts:
  - `player_classes`
  - `player_weapons`
  - `player_abilities`
- Runtime loadout now applies class HP / move speed and inserts the class ultimate into ability slot 4.
- Rebuilt the Bootstrap loadout UI around class selection:
  - class -> weapon from class pool -> three abilities from class pool
  - ultimate displayed as the B-slot in the summary and inserted automatically by `RunState`
  - same class remains allowed for multiple players
- Registered Slice 1 stubs for all forward-referenced class pool IDs:
  - weapons: `whirlwind`, `arc_wand`, `flamethrower`
  - abilities: `afterburn`, `momentum_burst`, `deflect`, `sonic_boom`, `ground_slam`, `quake`,
    `blood_lance`, `summon`, `reinforce`, `fireball`, `ignite`
  - ultimates: `slipstream`, `blood_frenzy`, `overload_grid`, `firestorm`
- Made all class-pool weapons, abilities, ultimates, and classes free in `ProfileState`.

### Validation
- `git diff --check`
- Headless parse:
  `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick_v4 --quit`
- Bootstrap smoke boot:
  `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick_v4 res://scenes/ui/Bootstrap.tscn --quit`
- PerfRunner:
  `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick_v4 -- --profile=champion:hive --players=2 --build=heavy`
  - Result: `avg_fps=144.4`, `min_fps=143.0`, `max_frame_ms=12.436`.

### Next
- Resume with Slice 2: kit-tag construction and mutation eligibility/schema rework.

## V4 class-system Slice 2 implemented

Implemented Slice 2 of `docs/development/v4-implementation-plan.md` in the
`D:\GameDev\Project_Twin_stick_v4` worktree on branch `v4/class-system`.

### What changed
- Converted `data/mutations.json` to the V4 mutation schema:
  - every mutation now has `requires` and `apply`
  - removed retired stat-stick mutations `rapid_fire`, `velocity`, `high_caliber`, and `range`
  - added Pierce as `piercing_rounds` with `requires:["projectile"]`
  - re-folded old ability-rares from `requires_ability` to tag requirements
  - dropped `decoy_volatile`
  - added the per-class mutation sets from the design doc
- Reworked `MutationSystem.gd` offer gating to build kit tags from class, passive, equipped weapon, equipped
  abilities, ultimate, and selected mutation tags.
- Kept mutation target application separate from offer gating through the new `apply` field.
- Removed in-run weapon-switch reward cards; weapon level-up remains available.
- Removed retired stat-stick compile paths from projectile and beam weapon stats.
- Preserved mutation projectile visuals after stat mapping so effect visuals are not overwritten by base
  projectile defaults.

### Validation
- `git diff --check`
- Mutation JSON parse:
  - `count=54 bad=0`
- Slice 2 tag acceptance:
  - projectile kit -> Pierce eligible
  - cone and melee kits -> Pierce ineligible
  - Risk kit -> Combustion eligible
  - non-Risk kit -> Combustion ineligible
  - no-summon kit -> Overgrowth ineligible
- Headless parse:
  `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick_v4 --quit`
- Bootstrap smoke boot:
  `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick_v4 res://scenes/ui/Bootstrap.tscn --quit`
- PerfRunner:
  `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick_v4 -- --profile=champion:hive --players=2 --build=heavy`
  - Result: `avg_fps=144.7`, `min_fps=143.0`, `max_frame_ms=19.251`.

### Next
- Resume with Slice 3: deployable persistence + HP for Turret, Orbit, Decoy, and player AbilityMine.

## V4 class-system Slice 3 implemented

Implemented Slice 3 of `docs/development/v4-implementation-plan.md` in the
`D:\GameDev\Project_Twin_stick_v4` worktree on branch `v4/class-system`.

### What changed
- Added `scripts/game/DeployableNode.gd` as the shared player-deployable HP/damage/death contract.
- Moved existing player deployables onto that contract:
  - `TurretNode.gd`
  - `OrbitNode.gd`
  - `DecoyNode.gd`
  - `AbilityMine.gd`
- Removed the lifetime countdown despawn paths from those player deployables.
- Kept Minefield detonation as a trigger behavior while HP death destroys the mine without detonating.
- Added explicit health values to ability data:
  - `decoy_health`
  - `turret_health`
  - `mine_health`
  - `orbit_health`
- Updated `CoopManager.gd` spawn/config calls for persistent deployables.

### Validation
- `git diff --check`
- Static no-lifetime check over player deployables:
  - no `lifetime -=`, `lifetime <=`, or `mine_lifetime` matches remain in `TurretNode.gd`, `OrbitNode.gd`,
    `DecoyNode.gd`, `AbilityMine.gd`, or `data/abilities.json`
- Temporary Godot acceptance script:
  - instantiated Turret, Orbit, Decoy, and AbilityMine
  - verified each joins `player_target`
  - verified each survives partial damage and dies on lethal damage
  - Result: `deployable_health_check=passed`
- Headless parse:
  `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick_v4 --quit`
- Bootstrap smoke boot:
  `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick_v4 res://scenes/ui/Bootstrap.tscn --quit`
- PerfRunner:
  `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick_v4 -- --profile=champion:hive --players=2 --build=heavy`
  - Result: `avg_fps=144.9`, `min_fps=144.0`, `max_frame_ms=6.903`.

### Next
- Resume with Slice 4: class passives.

## V4 class-system Slice 4 implemented

Implemented Slice 4 of `docs/development/v4-implementation-plan.md` in the
`D:\GameDev\Project_Twin_stick_v4` worktree on branch `v4/class-system`.

### What changed
- Added passive identity/state to `Player.gd` through compiled class loadouts.
- Made Momentum class-exclusive:
  - `MomentumTracker.gd` now only gains/restores/applies tiers for players with passive `momentum`
  - non-Mobile players are forced to Momentum tier `0`
- Added Tank Bloodthirst:
  - enemy damage can carry an optional source player index
  - weapon/projectile/beam and Orbit damage pass source attribution
  - kills credited to a Tank heal that player, with full-HP kills becoming decaying overshield
  - `gorge` and `overflow` mutations feed the first-pass heal/overshield numbers
- Added Controller Radiance:
  - summon/deployable ability stats get health/damage/fire-rate boosts for Radiance players
  - Radiance applies a nearby self/ally damage aura during room runtime
- Added Risk Overheat:
  - Risk ability cooldowns compile to `0.5s`
  - every ability cast builds Heat
  - Heat decays after a short idle delay
  - Heat increases outgoing damage and incoming damage taken
- Removed remaining runtime use of retired projectile mutation fields from `Player.gd` and `Projectile.gd`.
- Updated PerfRunner's heavy build seed to use current mutation ids.
- Fixed Split projectile activation to defer spawned follow-up projectiles out of physics collision flushing.

### Validation
- `git diff --check`
- Headless parse:
  `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick_v4 --quit`
- Bootstrap smoke boot:
  `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick_v4 res://scenes/ui/Bootstrap.tscn --quit`
- PerfRunner:
  `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick_v4 -- --profile=champion:hive --players=2 --build=heavy`
  - Result: `avg_fps=144.9`, `min_fps=144.0`, `max_frame_ms=6.903`.

### Next
- Resume with Slice 5: Whirlwind, Arc Wand, and Flamethrower real weapon behaviors.

## V4 class-system Slice 5 implemented

Implemented Slice 5 of `docs/development/v4-implementation-plan.md` in the
`D:\GameDev\Project_Twin_stick_v4` worktree on branch `v4/class-system`.

### What changed
- Replaced class weapon placeholders in `data/weapons.json`:
  - `whirlwind` now uses `projectile_kind:"melee"`
  - `arc_wand` now uses `projectile_kind:"chain"`
  - `flamethrower` now uses `projectile_kind:"cone"`
- Added base visual profiles for `melee`, `chain`, and `cone` weapon kinds.
- Added direct weapon-kind processors in `ProjectileSystem.gd`:
  - melee hits all enemies in radius and applies knockback
  - cone damages enemies inside a forward cone and applies a short burn DoT through the existing DoT hook
  - chain selects a forward target and jumps between nearby enemies with damage falloff
- Added lightweight runtime visuals for melee arcs, flame cones, and chain lightning.

### Validation
- `git diff --check`
- Temporary Godot acceptance script:
  - verified `melee` damages an in-radius fake enemy
  - verified `cone` damages only a forward fake enemy
  - verified `chain` damages first and chained fake enemies
  - Result: `weapon_kind_check=passed`
- Headless parse:
  `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick_v4 --quit`
- Bootstrap smoke boot:
  `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick_v4 res://scenes/ui/Bootstrap.tscn --quit`
- PerfRunner:
  `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick_v4 -- --profile=champion:hive --players=2 --build=heavy`
  - Result: `avg_fps=144.9`, `min_fps=144.0`, `max_frame_ms=6.944`.

### Next
- Resume with Slice 6: new class abilities and ultimates.

## V4 class-system Slice 6 implemented

Implemented Slice 6 of `docs/development/v4-implementation-plan.md` in the
`D:\GameDev\Project_Twin_stick_v4` worktree on branch `v4/class-system`.

### What changed
- Added `scripts/game/UltimateCharge.gd` as the per-player combat meter for Mobile/Tank/Controller ultimates.
- Wired ultimate charge from credited enemy damage and kills, with champion kills granting a larger fill.
- Added ultimate slot gating/reset in `Player.gd`:
  - uncharged ultimate presses are no-ops
  - Mobile/Tank/Controller readiness reads the combat meter
  - Risk Firestorm readiness reads Heat and vents Heat on activation
  - the existing ability HUD cooldown bar now shows ultimate charge/readiness
- Replaced Slice 1 ability/ultimate placeholders in `data/abilities.json` with first-pass stats.
- Added runtime behavior for the new abilities:
  - Afterburn, Momentum Burst, Deflect, Sonic Boom, Ground Slam, Quake, Blood Lance, Summon, Reinforce,
    Fireball, and Ignite
- Added runtime behavior for the ultimates:
  - Slipstream, Blood Frenzy, Overload Grid, and Firestorm
- Added `scripts/game/SummonNode.gd` as a persistent melee construct using the Slice 3 deployable HP contract.
- Threaded ability/fire-pool damage ownership through player shockwaves, fire zones, projectile fire pools,
  and new ability effects so combat charge and Bloodthirst attribution remain consistent.

### Validation
- `git diff --check`
- Temporary Godot acceptance script:
  - verified all new ability and ultimate ids have non-placeholder descriptions and non-empty stats
  - verified `UltimateCharge` fills from damage and resets the player-facing meter
  - Result: `ability_slice6_check=passed`
- Headless parse:
  `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick_v4 --quit`
- Bootstrap smoke boot:
  `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick_v4 res://scenes/ui/Bootstrap.tscn --quit`
- PerfRunner:
  `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick_v4 -- --profile=champion:hive --players=2 --build=heavy`
  - Result: `avg_fps=145.0`, `min_fps=144.0`, `max_frame_ms=6.944`.

### Next
- Resume with Slice 7: meta/unlock trimming and premium unlock entries.
