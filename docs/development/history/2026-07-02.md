# 2026-07-02

## Next-version class-based redesign — design completed (no code)

Pure design work; no code changed or committed. This continues the class-based pivot begun 2026-06-27..07-01.

### What happened
- Completed the full design of **all four classes** for the next version, each specified end-to-end:
  ultimate, 2 weapons, 6 skills, passive (with its base mechanic), and a mutation set.
  - **Mobile (Kinetic)** — Slipstream ult; rifle+beam; Momentum passive; kiting/movement-DPS.
  - **Tank (Blood/Juggernaut)** — Blood Frenzy; shotgun + Whirlwind (new melee); on-kill Bloodthirst.
  - **Controller (Arcane)** — Overload Grid; beam + Arc Wand (new chain-lightning); Radiance + deployables.
  - **Risk (Ember)** — Firestorm; Flamethrower (new cone) + rocket; Overheat (heat → dmg + glassier).
- Locked the supporting systems: **kit = 1 weapon + 4 abilities** (LT/RT/LB/RB), **dash as the universal
  mobility hub** (variants are mutations), **mutation/tag architecture** (scope universal/class; element-only
  tags on weapons+abilities; reuse the `PER_TAG_RATE` amplify system + `requires_ability` targeting),
  **all deployables persistent + HP**, **meta = all classes free, premium build content unlocks**, and the
  **art path** (abstract now → billboard rubberhose later, presentation-only, no gameplay rework).
- Three new weapons authored in design: Whirlwind, Arc Wand, Flamethrower.

### Deliverables
- **`docs/design/class-system-redesign.md`** — the review-ready prose spec (rationale, systems, all 4
  classes, new-content inventory, implementation touchpoints, open items).
- **`docs/design/class-design.xlsx`** — the live data grid (Kits / Mutations / Reference tabs).
- `start-of-day.md` updated to point at the new spec.

### Open / next
- Universal mutation lists (elements, dash-hub variants) and all tuning numbers are still placeholders.
- Bosses-in-swarm rework still deferred.
- **Next session:** review the design spec, then produce a Codex-ready implementation plan (candidate slice
  order in §8 of the spec: kit 2→4 → class data model + loadout UI → per-class content → mutation/tag rework
  → meta/unlocks).
