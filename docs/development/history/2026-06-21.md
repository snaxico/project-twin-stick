# 2026-06-21

## Post-implementation code review + minor hardening fixes

Reviewed the implemented structure rework against the spec and real code. **Verdict: correct and
faithful** — unique node IDs, single-owner advancement (select sets current; resolve advances),
champion cadence + no-repeat bag in `RunState`, momentum persisted/restored through `RunState`,
depth-based rare odds + modifier ramp, fixed arena color, champion-into-wave, milestone Continue/End-Run,
and continuation `get_run_progress() > 1.0` (soft-capped) all verified. Cleanup grep over runtime paths
returned zero dangling symbols. Headless parse + boot clean.

Fixed the three minor (P3) findings:
- **Unbounded `node_map`/lookup growth** — `_advance_to_next_step` now keeps only the current step
  (`node_map = [step]`, mirroring the old endless pattern; the clamp in `_get_current_step_node_ids`
  handles the >0 step index) instead of appending forever + rebuilding the whole lookup each room.
- **Vestigial `"elite"` branch** removed from `RunFlow._on_next_room_card_pressed` (no node produces it).
- **Defensive champion-clear guard** in `_check_wave_progress`: force the champion to spawn once
  spawning is done, so a champion room can never clear without the champion having appeared and died
  (removes reliance on the spawn-delay < room-duration invariant; prevents a future soft-lock).

Validation: `git diff --check` clean; headless parse + boot (`--quit-after 1`) clean.

## Phase 4 tuning after approved playtest

User approved the structure-rework playtest and asked to continue with Phase 4. Applied a conservative
numeric tuning pass without adding new structure.

- Added a soft-capped continuation pressure curve past room `10` instead of clamping `get_run_progress()`
  at the milestone.
- Continued deep-run escalation through spawn interval, room duration, burst interval, burst size, and
  deeper enemy-pool weighting.
- Retuned champion scaling toward threat over HP sponge:
  - lower base HP and lower depth HP growth
  - modest depth-based contact/projectile damage growth
  - depth-based champion cooldown tightening
  - slight depth-based champion speed lift
- Separated Cannon and Beam roles:
  - Cannon now has splash damage/radius and slightly lower direct-hit damage, pushing it toward burst-AOE.
  - Beam keeps sustained tracking DPS but ramps more slowly and has a lower late-level DPS ceiling.
- Cleaned champion HUD labels so former elite IDs display as readable champion names.

Validation:
- `git diff --check` clean.
- Godot headless parse clean.
- Runtime cleanup gates over `scripts scenes data project.godot` still return zero hits for removed
  map/endless/mode/phase/elite-tier symbols.
- PerfRunner champion dense-wave profile completed:
  - `--profile=champion:hive --players=2 --build=heavy`
  - average `144.9 FPS`, max frame `6.944 ms`, Hive attack markers emitted.

## Implemented structure rework Phases 1-3

Implemented the locked "trim" structure rework on `v3/structure-rework`.

### Phase 1 - map / endless / mode-select removal

- Replaced branching map/endless generation in `RunState.gd` with lazy linear room generation.
- Normal steps generate two route cards; champion steps generate one forced card.
- Removed act/endless concepts from runtime generation and scaling gates.
- Removed the pre-run Structured / Endless selector from `Bootstrap`.
- Replaced `RunFlow` map graph UI with a `NextRoomPanel`.
- Deleted unused `scripts/ui/MapNodeButton.gd`.
- Removed stale `wave_count` node data.

### Phase 2 - champions

- Collapsed boss/elite presentation into the champion tier while keeping stable internal enemy IDs.
- Champion pool now includes Warden, Hydra, Hive, Pulsar, Charger, Spitter, and Support.
- Champion rooms are live wave rooms with a champion spawned partway through.
- Removed boss phase transitions, phase HUD pips, boss entrance windup, boss add-wave budget, elite rooms,
  elite add-waves, and elite act scaling.
- Trimmed former boss behavior toward the locked 2-attack champion kits.
- Updated Encounter Builder, Encyclopedia, debug spawn labels, and PerfRunner taxonomy to champion naming.

### Phase 3 - milestone + momentum persistence

- Clearing room `10` banks a win and shows a milestone resolution with `Continue` and `End Run`.
- Continue advances to room `11` and keeps the same run scaling.
- Death after the milestone no longer overwrites the banked win state.
- Momentum tier/progress now lives in `RunState`, restores into each new room, and writes back on gain/loss.

### Validation

- `git diff --check` clean.
- Godot headless parse clean.
- Runtime cleanup gates over `scripts scenes data project.godot` returned zero hits for removed
  map/endless/mode/phase/elite-tier symbols.
- PerfRunner champion dense-wave profile completed:
  - `--profile=champion:hive --players=2 --build=heavy`
  - average `144.1 FPS`, max frame `26.642 ms`, attack markers emitted for Hive deflectors and poison cloud.

## Resolved the parked critical-review open items

Continued from the 2026-06-20 critical system review, which had parked design calls + mechanical fixes
in the structure-rework plan. Walked them as multiple-choice and folded the results in.

### Design calls (decided)

- **HP pickups → KEEP as-is** (10% drop / +5 HP intra-room safety net). No change.
- **Side objectives → KEEP as-is** (Hold Zone / Kill Streak / Collector + room buffs). No change.
- **Momentum → BUILD ACROSS THE RUN** (was per-room reset). Drop the `_reset_momentum()` call in
  `_start_room()` so tier/progress persist room-to-room and reset only at run start; the −2-tier hit
  penalty stays. Makes flow run-scoped (rewards the continuation/score-chase). Slotted into **Phase 3**.
  Updated the "Run stake" framing: health still resets per room, but momentum is now the one thing that
  carries (a power snowball you can't bank for safety).

### Mechanical fixes (decided, folded into Phase 1)

- **Rare-odds → depth curve.** Replace `_get_current_rare_chance()` (keyed off the deleted
  `is_endless_mode()` + binary act, which stalled at 0.30 forever) with odds rising with `depth`.
- **Enemy pool → one depth-based curve** (fold `_build_enemy_pool` into the depth-banded pool).
- **"Acts" → generalized FULLY to depth** (chosen over cosmetic bands). Drop `current_act` /
  `get_current_act` / `set_current_act` / `ROOMS_PER_ACT` / the node `act` field; rework
  `_apply_arena_color_for_act` into a depth-based arena color. Difficulty, pool, rare odds, and color all
  key off `depth` continuously — no binary act, no freeze at "act 2".

### Plan housekeeping

- Swept the whole plan for stale `act` / "two acts" / per-room-momentum wording and reconciled it.
- Added the act/rare-odds/pool/arena-color removals to the Phase 1 cleanup audit; added the momentum
  change to Phase 3.

**Status:** structure-rework plan Phases 1–3 Codex-ready, Phase 4 = tuning-last. All committed + pushed.

## Audit pass — debugger / encounter builder / perf / weapons / HUD / mutations

Audited each against the new features (user request). Verdicts:
- **Debugger (F4 overlay):** fine; only the spawn catalog (`DEBUG_ENEMY_SPAWN_CATALOG`) needs the champion
  relabel (already in UI audit). Pre-existing nit: debug weapon dropdown lists `shockwave` (a
  `primary_skill` in weapons.json) — optional cleanup.
- **Encounter Builder + PerfRunner:** usable after the already-planned taxonomy edits (room-type/champion
  dropdown; `boss:`→`champion:` + champion-in-dense-wave). No new gaps.
- **Weapons:** none broken. Real find — **Cannon ↔ Beam niche overlap**: Beam ramps on a single tough
  target so it's now the champion-killer, overlapping Cannon's old boss-killer role. Rethink Cannon in
  the Phase-4 re-tune. Other five are wave-friendly.
- **HUD:** all issues already captured; momentum-persist is display-fine.
- **Mutations/items:** no structural rework (none reference bosses/elites/modes/acts). Two notes:
  **upgrade-pool depth** (9 commons cap-3 + rares → deep runs go hollow — **decided LEAVE FOR NOW**,
  parked, revisit post-playtest) and **mutation×momentum-build×depth balance** (Phase-4 tuning).

Added Cannon↔Beam + balance to "Notes for the re-tune", the upgrade-pool + debug-dropdown to "Parked",
and an "Audit confirmed covered" line.

## Branch + made Phase 1 truly Codex-ready

- **Branch:** rework now lives on **`v3/structure-rework`** (created from `v3/main` @ `0402799`, pushed).
  `v3/main` frozen as the stable game. User chose a plain branch over worktree/fork (understood the
  difference). Updated the worktree/branch memory + plan header.
- **Reviewed Codex-readiness critically** — Phase 1 was over-scoped + had 5 loose specs. Traced all to
  real code. Findings: the **2-card UI is basically free** (`_rebuild_map_graph` already renders
  `get_current_options()` as a centered card row); but **"drop acts" ripples into `_roll_modifiers_for_node`
  and `_apply_arena_color_for_act`** (both act-keyed) — flagged rather than assumed.
- **Decided (MC):** arena color → **single fixed neon** (drop depth-driven color); modifier intensity →
  **continuous depth ramp**.
- **Concretized every spec:** new `_build_run_node` (replaces graph-oriented `_build_map_node`, reuses
  depth-banded `_get_endless_enemy_pool`/`_get_endless_wave_count`); exact `_is_champion_step` (track
  last-champion depth, step by band interval — not `% interval`); `_roll_modifiers_for_depth` formula;
  rare-odds `lerpf(0.20,0.45,…)` curve; fixed `_apply_arena_color()`; 2-card UI = existing card row.
- **Sliced Phase 1 into 1a/1b/1c** (RunState gen / CoopManager depth fixes / UI+scene+cleanup), each
  with its own acceptance gate. Reconciled all stale "act"/"modifiers unchanged" wording.

**Phase 1 is now genuinely Codex-ready — 1a is the first hand-off.**

## Plan review — fixed contradictions before hand-off

An external plan review flagged contradictions my iterative edits introduced. Verified the code-dependent
claims, then fixed all of them:
- **Verified `wave_count` is dead** (`CoopManager` has zero refs) — removed it from the new node builder
  and removed `_get_endless_wave_count`; added a **continuation-scaling tuning gap** note (cadence
  clamps at `RUN_LENGTH`, `wave_count` dead → deep play only gains enemy *variety*; Phase-4 must add a
  real deep escalation driver).
- **Verified `GameWorld`/`CoopManager` is re-instantiated per room** ([RunFlow.gd:310](scripts/ui/RunFlow.gd:310))
  → **momentum-build-across-run is NOT "remove the reset"**; rewrote the spec: store momentum in
  `RunState`, restore on room start, write back on gain/loss, reset in `start_new_run`.
- **Scene/UI conflict (decided REPLACE):** build a new Next-Room panel + delete old map nodes /
  `_show_map` / `_rebuild_map_graph`; reconciled cleanup audit + UI audit + 1c.
- **Rare-odds + arena-color contradictions** reconciled (depth curve; single fixed neon — not depth color).
- **Champions forced (user clarified):** no optional/choosable champion; champion step = one forced
  "Champion incoming" card, no skip.
- **Elite IDs clarified:** keep `elite_charger/spitter/support` type IDs internally; cleanup greps the
  elite *tier* symbols, never `elite_` broadly.
- **Boss-room placeholder explicit:** Phase 1 keeps current boss-room clear behavior unchanged; the
  "champion drops into a wave" behavior is Phase 2.

Next: implement 1a on `v3/structure-rework`.
