# 2026-06-27

## CoopManager decomposition started

Started the `CoopManager.gd` decomposition on `v3/structure-rework` with the low-risk phases from
`docs/development/coop-manager-decomposition-plan.md`.

### Phase 0 cleanup

- Removed dead `RunState.run_mode`, `is_easy_mode()`, and `is_run_complete()` leftovers after confirming
  there were no live code callers.
- Added a diagnostic warning for unknown `ProfileState.is_unlocked()` IDs while preserving the existing
  unlocked-by-default return behavior.
- Left optional Hive deflector timer cleanup unimplemented for now because it is behavior-adjacent and not
  required for the structural decomposition.

### Phase 1 stateless helpers

- Added `CoopFormat.gd` for room/boss/modifier/buff formatting, slot colors, overbright color, and rarity
  rank.
- Added `EnemyTypes.gd` for champion enemy ID normalization and champion classification.
- Added `ArenaGeometry.gd` for segment distance, player/enemy/champion spawn-position math, and projectile
  spread directions.
- `CoopManager.gd` still owns runtime orchestration and the combat-owner facade; these extractions do not
  move any stateful gameplay ownership.

### Validation

Each code slice passed:

- `git diff --check`
- Godot headless parse:
  `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick --quit`
- Bootstrap scene headless smoke boot:
  `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick res://scenes/ui/Bootstrap.tscn --quit`
- PerfRunner Hive champion profile:
  `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick -- --profile=champion:hive --players=2 --build=heavy`

## ProjectileSystem extracted

Completed T3 of the CoopManager decomposition.

- Added `ProjectileSystem.gd` as a CoopManager-owned child node.
- Moved projectile pool state, active projectile/homing lists, beam state, projectile renderer ownership,
  player/enemy projectile fire bodies, split/deactivation callbacks, projectile impact handling, homing orb
  updates, and projectile hit VFX into the new subsystem.
- Kept `CoopManager.gd` as the signal entry point for player/enemy fire requests and as the public
  `spawn_enemy_homing_orbs(...)` combat-owner facade.
- Added `CoopManager.get_enemy_count()` so the projectile subsystem can preserve the existing combat VFX
  suppression threshold without owning `_enemy_nodes`.

Validation: `git diff --check`, Godot headless parse, Bootstrap scene boot, and PerfRunner
`champion:hive --players=2 --build=heavy` all passed.

## Nearest-player lookup centralized

Follow-up cleanup after the T3/T4 review:

- Promoted `CoopManager.get_nearest_player_to(origin)` as the single nearest-live-player lookup.
- Removed the duplicate private lookup body from `ProjectileSystem.gd`; homing orb spawn/retargeting now
  calls back through CoopManager, matching the target-accessor boundary used elsewhere.

Validation: `git diff --check`, Godot headless parse, Bootstrap scene boot, and PerfRunner
`champion:hive --players=2 --build=heavy` all passed.

## ArenaVisuals extracted

Completed T4 of the CoopManager decomposition.

- Added `ArenaVisuals.gd` as a CoopManager-owned child node.
- Moved floor polygon setup, floor grid rebuild, arena wall visuals, collision-bound sizing, arena color
  application, and grid pulse/highlight logic into the subsystem.
- Passed the full setup-node contract into `ArenaVisuals`: floor, grid, four arena walls, exit zone,
  exit-zone shape/visual, camera, and arena constants.
- Kept the grid pulse at the first `_physics_process` step, before mutation-pick and pause early returns.

Validation: `git diff --check`, Godot headless parse, Bootstrap scene boot, and PerfRunner
`champion:hive --players=2 --build=heavy` all passed.

## GameHud extracted

Completed T5 of the CoopManager decomposition.

- Added `GameHud.gd` as a CoopManager-owned child node.
- Moved in-room HUD construction and refresh out of `CoopManager.gd`: XP/score/room labels, boss health,
  side-objective panel, modifier chips, player combat indicators, revive markers, and bottom player cards.
- Kept `CoopManager.gd` wrapper call sites for `_refresh_hud()`, `_refresh_boss_hud()`,
  `_update_player_combat_indicator_positions()`, `_populate_modifier_hud()`, and objective text formatting.
- Added read-only CoopManager accessors for room, boss, player, revive, modifier, momentum, and side-objective
  state so `GameHud.gd` does not read manager fields directly.
- Left clear-summary construction in `CoopManager.gd` for T7, per the decomposition plan.

Validation: `git diff --check`, Godot headless parse, Bootstrap scene boot, and PerfRunner
`champion:hive --players=2 --build=heavy` all passed.

## WaveDirector extracted

Completed T6 of the CoopManager decomposition.

- Added `WaveDirector.gd` as a CoopManager-owned child node.
- Moved spawn cadence/scaling/progress state, opening burst, stream/burst spawning, deferred enemy spawn
  queueing, champion spawn timing, and the active boss pointer into the director.
- Kept `_room_elapsed` on `CoopManager.gd` as the shared room clock; `WaveDirector.gd` reads it through
  `CoopManager.get_room_elapsed()`.
- Kept `_enemy_nodes` single-owned by `CoopManager.gd`; `WaveDirector.gd` registers spawned enemies back
  through `CoopManager.register_enemy(...)` and uses `is_enemy_list_empty()` / `get_enemy_count()` reads.
- Enemy setup still receives the `CoopManager` combat-owner facade, and enemy death/fire/hit signals still
  connect to the manager callbacks.
- Repointed boss/duration/spawning HUD accessors to the director-owned state and added the active-boss
  death notification contract.

Validation: `git diff --check`, Godot headless parse, Bootstrap scene boot, and PerfRunner
`champion:hive --players=2 --build=heavy` all passed.
