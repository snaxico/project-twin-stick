# 2026-07-03

## V4 class-system Slice 0 implemented

Implemented the first vertical slice of `docs/development/v4-implementation-plan.md` in the
`D:\GameDev\Project_Twin_stick_v4` worktree on branch `v4/class-system`.

### What changed
- Added `data/classes.json` and `scripts/game/ClassRegistry.gd`; `RunState` now reloads class data.
- Added `tags` fields to existing weapons and abilities.
- Expanded `PlayerInventory`, `AbilityRegistry`, `RunState`, `CoopManager`, and `Player` from two ability
  slots to four runtime ability slots.
- Replaced P1/P2 runtime ability input with `p%d_ability_1..4`.
  - Controller order is `A`, `X`, `Y`, `B`.
  - Keyboard defaults are number-row `1`, `2`, `3`, `4`.
  - In-run weapon-switch actions for P1/P2 were removed from `project.godot`.
- Updated Options binding rows and the combat HUD / pause build overlay to show four ability slots.
- Kept the existing pre-run ability picker as the temporary Slice 0 input for the first two ability choices;
  `RunState` expands that to the default four-ability runtime loadout until Slice 1 adds class loadout UI.
- Fixed the design-doc source-of-truth note so the `.md` is authoritative and `class-design.xlsx` is
  human-facing only.

### Validation
- `git diff --check`
- Headless parse:
  `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick_v4 --quit`
- Bootstrap smoke boot:
  `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick_v4 res://scenes/ui/Bootstrap.tscn --quit`
- PerfRunner:
  `Godot_v4.6.2-stable_win64_console.exe --headless --path D:\GameDev\Project_Twin_stick_v4 -- --profile=champion:hive --players=2 --build=heavy`
  - Result: `avg_fps=144.9`, `min_fps=144.0`, `max_frame_ms=6.944`.

### Next
- Stop here for the session break.
- Resume with Slice 1: class-aware inventory/loadout UI, forward-reference stubs, and base-kit unlock handling.
