# 2026-05-04

## Summary

- Created the Godot project foundation, process docs, development-memory docs, and Git baseline.
- Implemented Phase 0 plus the Patch 0 room/movement baseline in a clean placeholder-only setup.
- Implemented Patch 1 through Patch 7 across co-op input, combat, secondary fire, modifiers, room flow, shared build progression, revive flow, and the boss endpoint.
- Implemented an early Patch 8 baseline with `1–4` player support, more layouts, more modifiers, and light placeholder-only juice.
- Implemented a Patch 9 baseline with persistent meta-gold rewards, unlock purchases, save data, and a return-to-menu loop for spending persistent progression.
- Added a dedicated run-end summary panel that highlights meta-gold gains and newly affordable unlocks before handing off to the meta menu.
- Started the juice-and-polish phase with `J1`: enemy/player flash feedback, enemy knockback, delayed death flash, and kill hitstop.
- Added `J2`: trauma-based screen shake, grenade explosion camera punch, and camera feedback hooks for kills, downed states, and room clear.
- Synced the docs to the actual current project state.

## Notes

- The project currently runs as a same-screen local co-op prototype with a bootstrap menu, run-flow scene, combat rooms, room modifiers, shared gold, shared loadouts, revive flow, and a placeholder boss endpoint.
- Placeholder visuals remain Godot-native with no custom sprite production.
- Headless Godot validation from this environment is unreliable because local executable access outside the workspace is blocked.
- The next work should still be tuning, validation, and eventual export/distribution flow rather than more combat-scope expansion.
## J3 Particle Effects

- Added `scripts/juice/ParticleFactory.gd` to create reusable `GPUParticles2D` bursts and projectile trails.
- Added `Effects` to the room scene so one-shot particles outlive their spawning projectile or enemy.
- Wired player muzzle flash and dash trail, projectile trail and impact sparks, enemy death burst, and grenade explosion burst.
- Preserved the earlier grenade readability correction: no full-screen grenade shake, preview only while the input is held and the secondary is ready.

## J4 Procedural Audio

- Added `scripts/juice/SfxEngine.gd` as a shared procedural audio runtime with an `AudioStreamGenerator` player pool.
- Wired combat SFX into `CoopManager.gd` for fire, hit, explosion, dash, damage, enemy death, and room clear events.
- Wired UI click sounds into the bootstrap and run-flow menus.
- Kept the patch asset-free: all sounds are generated from math at runtime.

## J5 UI Polish

- Added `scripts/juice/HealthBarHUD.gd` for tweened health bars with a delayed ghost bar.
- Added `scripts/juice/FloatingText.gd` for combat damage numbers and gold reward text.
- Replaced HP-in-text usage with player health bars and a boss health bar in the combat HUD.
- Added low-time pulse feedback to the survival timer text and button/panel motion to the bootstrap and run-flow UI.

## J6 Entity Animation Polish

- Added player movement lean, turn squash, aim-line recoil, and downed alpha pulsing in `scripts/player/Player.gd`.
- Added enemy idle bob, movement tilt, and spawn-in pop/fade in `scripts/enemies/Enemy.gd`.
- Added explicit projectile facing updates in `scripts/weapons/Projectile.gd`.
- Added player spawn-in easing and revive burst particles in `scripts/game/CoopManager.gd`.

## J7 Environmental Polish

- Added `scripts/juice/ScreenEffects.gd` and mounted it in the combat scene for vignette, low-health edge pulse, and combat warmth overlays.
- Added subtle floor shader noise to the arena floor and smoothed room modifier tint transitions in `scripts/game/CoopManager.gd`.
- Added enemy spawn warning pulses plus wave/adds announcement text before delayed room spawns.
- Added a room-transition wipe overlay in `scripts/ui/RunFlow.gd` so map, room, resolution, choice, and summary transitions no longer cut instantly.

## Arena Readability Pass

- Replaced the slanted arena presentation with a cleaner rectangular grid floor and plain wall blocks.
- Pulled the camera closer so the room occupies more of the screen again.
- Increased player and enemy silhouette size toward a chunkier Brotato-like read.
- Brightened the arena palette and increased projectile contrast so bullets stand out more clearly in combat.

## Enemy Behavior Pass

- Reworked chasers into a stalk-and-lunge pattern instead of constant drift pressure.
- Reworked spitters into strafing burst shooters that hold range more deliberately.
- Added a new charger archetype with windup plus straight-line dash pressure.
- Updated normal combat waves and boss support waves so the new archetype mix appears in regular play.

## Secondary Identity Pass

- Kept `Grenade` as the standard aimed throw.
- Reworked `Cluster` into a quicker canister that bursts into multiple nearby blasts.
- Reworked `Siege` into a heavier aimed shell that detonates in repeated pulses.
- Updated player secondary handling and run-state loadouts so the behavior difference is mechanical, not just stat-based.

## Debug Start Mode

- Added a bootstrap debug mode toggle with starting primary and secondary selectors.
- Wired debug start overrides through `RunState.start_new_run()` so test loadouts can be forced without changing the normal run progression path.
- Normalized the current secondary input flow so `Cluster` uses the same hold-to-aim HUD behavior as the other secondaries.
